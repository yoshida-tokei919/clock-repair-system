# CLAUDE.md — "Spinning Voyager" プロジェクト ガイド

このファイルはAIアシスタント（Claude Code）がプロジェクトを操作する際の絶対ルールを定めます。
**いかなるファイル編集の前にも、必ずこのファイルを読み込み、以下の3点を宣言すること：**
1. どの条項に沿って作業するか。
2. 日本語以外の表記（英語）が混入しないことの確認。
3. つぎはぎ（パスタ）的な場当たり的修正を行わず、本質的な解決を誓約する。

---

## 0. UI変更後の動作確認（Playwright MCP）

**UI・画面に関わる変更を行ったあとは、必ず Playwright MCP を使って実際の画面で動作確認を行うこと。**

### 確認手順
1. 開発サーバーが起動していることを確認（`npm run dev` / ポート 3000）
2. Playwright MCP で変更した画面を開く
3. 変更内容が正しく表示・動作しているかスクリーンショットで確認
4. 問題があれば修正してから git push する

### 確認対象の例
- フォーム項目の追加・変更 → 全項目が表示されているか、送信が通るか
- レイアウト変更 → 崩れていないか
- ボタン・リンクの追加 → 遷移先が正しいか
- 一覧・詳細画面の変更 → データが正しく表示されているか

### 確認が不要なケース
- Prismaスキーマ・マイグレーションのみの変更
- 型定義・ユーティリティ関数のみの変更
- ユーザーが画面で体感できない内部処理のみの変更（例：ログ出力、キャッシュ設定）

### 判断基準
「APIルート・サーバーアクションのみの変更だから不要」ではなく、**「ユーザーが画面で体感できる動作が変わるか」** を基準にする。
ロジック変更でも画面上の挙動（ステータス変化・表示内容・操作結果）に影響する場合は必ず確認する。

---

## 1. 顧客・パートナー管理

### 共通ルール
- 顧客IDのフォーマット: `[PREFIX]-[SEQ]`
- SEQ はプレフィックスグループごとに自動インクリメント。

### 法人パートナー（B2B）
- 新規登録時、ユーザーが手動でユニークなプレフィックス（1〜3文字）を入力できること。
  - 例: "TRUST" → プレフィックス "T" → ID "T-001"
  - 例: "Brand Ichiban" → プレフィックス "BR" → ID "BR-001"
- 登録ダイアログに「プレフィックス」入力フィールドを設けること。

### 個人顧客（B2C）
- プレフィックスは **"C"** 固定。
- 例: "C-001", "C-002", "C-003"…
- UIではプレフィックス入力を非表示または無効化（"C" 固定）にすること。

---

## 2. UI/UX の保全とローカライゼーション

- **完全日本語化の徹底**: UI上のあらゆる表記から英語を排除し、日本語のみとする。
  - `顧客情報 (Client)` → `顧客情報`
  - `見積中 (Diagnosing)` → `見積中`
  - 括弧書きの英語併記も禁止。プログラム内部の識別子（`reception` 等）が画面に漏れ出さないよう、必ず変換処理を通すこと。
- **パフォーマンスとシンプルさの追求**:
  - 不要な装飾や重い処理を避け、FileMakerのような軽快な動作を目指す。
  - データベースからの取得データは必要最小限に絞り、不要な通信（Includeの多用）を抑制する。
- **日本時間（Asia/Tokyo）の徹底**: 日時・タイムスタンプは常に日本時間で表示する。
- **重要機能の保護**: 明示的な指示なく、既存のボタン・ダイアログ・入力フィールドを削除しないこと。
- **登録フロー**: 顧客名入力欄の横にある「新規登録」ボタンは常に詳細登録ダイアログ（`QuickRegisterDialog`）を起動しなければならない。
- **レイアウト変更時**: リファクタリング（例: 3カラム化）においても、全ての機能要素を保持・移動させ、削除しないこと。

---

## 3. マスタデータの自動永続化

- 修理を新規作成・更新した際、見積セクションに入力された「作業工賃」「部品」は、`PricingRule`（作業マスタ）・`PartsMaster` に該当データがなければ自動登録・更新すること（時計：ブランド/モデル/キャリバー単位）。
- ブランド・モデルの表示は日本語名（`nameJp`）を優先し、空の場合のみ `name`（英語）にフォールバック。
- モデルと型番（Ref）は正確な部品照合のために必須項目とする。

---

## 4. コマンド

- **「ルール追加」**: ユーザーが「ルール追加」と指示したら、即座に新ルールをこのファイルへ追記すること。

---

## 5. データベーススキーマ（変更禁止の参照定義）

以下のスキーマはコアデータ構造の定義。明示的な要求なくして変更禁止。

```prisma
// 1. 顧客管理
model Customer {
  id           Int      @id @default(autoincrement())
  type         String   // 'individual', 'business'
  rank         Int      @default(1) // 1〜5
  name         String
  kana         String?
  companyName  String?
  zipCode      String?
  address      String?
  phone        String?
  email        String?
  lineId       String?
  passwordHash String?
  createdAt    DateTime @default(now())

  // パートナー拡張
  isPartner    Boolean  @default(false)
  prefix       String?  // 例: "T", "JK"
  currentSeq   Int      @default(0) // 最終発番SEQ

  // 帳票SEQ
  seqEstimate  Int      @default(0)
  seqDelivery  Int      @default(0)
  seqInvoice   Int      @default(0)

  watches       Watch[]
  repairs       Repair[]
  deliveryNotes DeliveryNote[]
  invoices      Invoice[]
  estimateDocuments EstimateDocument[]
}

// 2. 修理・時計
model Repair {
  id                   Int       @id @default(autoincrement())
  uuid                 String    @unique @default(uuid())
  inquiryNumber        String    @unique // 例: T-001
  customerId           Int
  watchId              Int
  partnerRef           String?   // 業者管理番号（手入力）
  accessories          String?   // 付属品（JSON or CSV）

  receptionDate        DateTime  @default(now())
  status               String    // 'reception', 'diagnosing' 等
  approvalStatus       String    @default("pending")

  // スケジュール・優先度
  priorityScore        Int       @default(0)
  scheduledDate        DateTime?
  estimatedWorkMinutes Int       @default(0)

  workSummary          String?
  internalNotes        String?

  // SEOフラグ
  isPublicB2C          Boolean   @default(false)
  isPublicB2B          Boolean   @default(false)
  publicTitle          String?
  publicDescription    String?
  endUserName          String?   // B2Bエンドユーザー名

  deliveryDateExpected DateTime?
  deliveryDateActual   DateTime?

  createdAt            DateTime  @default(now())
  updatedAt            DateTime  @updatedAt

  customer Customer          @relation(fields: [customerId], references: [id])
  watch    Watch             @relation(fields: [watchId], references: [id])
  logs     RepairStatusLog[]
  photos   RepairPhoto[]
  estimate Estimate?
}
```

---

## 6. 内装・外装の定義と修理ロジック

### 絶対定義（内装と外装の境界線）

| 区分 | 定義 | 具体例 | 依存先 |
|------|------|--------|--------|
| **内装** | ムーブメント（機械体）への全修理作業・部品 | 分解掃除（OH）、ゼンマイ交換、歯車交換、注油、歩度調整 | キャリバー |
| **外装** | ムーブメント以外への全修理作業・部品 | ケース、ブレスレット、ガラス、リューズ、文字盤、針、パッキン、研磨 | モデル/Ref（型番） |

### 自動転記（Lookup）の掟（2段階階層方式）

1. **第1段階（個別価格）**: `[ブランド]` + `[キャリバー]` + `[作業/部品名]` で検索。
2. **第2段階（標準価格）**: 第1段階で未発見の場合、`[ムーブメント製造元（ETA等）]` + `[キャリバー]` + `[作業/部品名]` で共通価格を適用。

### 部品の紐付け管理

- **内装部品**（ゼンマイ、ローター等）: `[ブランド+キャリバー]`、なければ `[製造元+キャリバー]`
- **外装部品**（文字盤、針等）: 以下の優先順位で検索
  1. `[ブランド]` + `[モデル名]` + `[型番/Ref]` + `[部品名]` + `[備考/仕様]`
  2. `[ブランド]` + `[モデル名]` + `[部品名]` + `[備考/仕様]`
  3. `[ブランド]` + `[部品名]` + `[備考/仕様]`
- **汎用部品**（パッキン、汎用ガラス、リューズ、バネ棒等）:
  - `[ブランド：なし/共通]` + `[部品名]` + `[備考/仕様（サイズ、形状等）]`

### 部品の属性による厳密な識別

- 同一名称でも **「備考/仕様」**（サイズ、色、純正/汎用、新品/中古等）が異なる場合は別マスタデータとして管理。
- 外装汎用部品は「サイズや仕様」が検索の主キー。

### 入力の尊重と自動学習

- 候補は提示するが、勝手に選択しない。ユーザーが選ぶかBLURした時点で値を確定する。
- 手入力された「部品名＋備考/仕様」とその金額は、修理保存時にマスタへ自動保存・更新する。

---

## 7. AIの動作と品質の掟

- **爆速レスポンス（平坦化インデックス）**:
  - ブラウザのメモリ上では、Lookupを0.1秒以下で行えるよう「一発で引けるシンプルな価格索引（平坦化）」に変換して保持すること。
  - サーバー通信を待たずに即時検索できること。
- **作業前の宣言義務**: ファイル編集前に必ずこのファイルを読み、作業条項・日本語化・根本解決の3点を宣言する。

---

## 8. マスタデータの構造定義 V2

**原則**: 内装・外装は同一テーブル内の **「区分（Category）」フラグ** で管理する。物理テーブルは分割しない。

### 1. 作業工賃マスタ（PricingRule）

| 項目 | 必須 | 備考 |
|------|------|------|
| 区分（内装/外装） | ○ | |
| ブランド | ○ | |
| 作業名 | ○ | |
| 標準工賃 | ○ | |
| 時計Ref | — | 外装用紐付けキー |
| キャリバー | — | 内装用紐付けキー |
| ムーブメント製造元 | — | 内装標準用 |

### 2. 部品マスタ（PartsMaster）

| 項目 | 必須 | 備考 |
|------|------|------|
| 区分（内装/外装/共通） | ○ | 検索の第一条件 |
| ブランド | ○ | 汎用部品は「共通」等 |
| 部品名 | ○ | 検索メインキー |
| 部品Ref | ○ | メーカー部品番号（空欄可だがカラムは必須） |
| モデル名 | ○ | Ref不明時の目合わせ検索用 |
| 備考/仕様 | ○ | サイズ、色、純正/汎用等の識別キー |
| 仕入れ価格 | ○ | 管理者のみ閲覧・編集可 |
| 上代（定価） | ○ | 顧客提示用価格 |
| 時計Ref | — | 任意紐付け |
| キャリバー | — | 任意紐付け |

### インテリジェンス・キャッシュの原則

- マスタデータ（静的）は即時表示。膨大な履歴データ（動的）は必要時に高速取得。
- 5万件超のデータは「目次検索」と「詳細取得」を分離し、バックグラウンド通信でレスポンスを確保する。

---

## 9. 帳票・ツール類の完全仕様

### 共通ルール

- **B2B向け**: リスト形式（複数行）
- **B2C向け**: 詳細形式（写真可）
- **ヘッダー**: 宛名（〇〇御中/様）＋自社情報（屋号/住所/TEL/登録番号）を全帳票に記載。
- **一括発行**: 修理一覧から複数選択し、1枚のPDFにまとめる機能を実装すること。

### 各帳票の個別仕様

1. **御見積書（Estimate）**
   - 形式: B2Bリスト形式
   - 必須項目: `シリアルNo`、`お客様名（エンドユーザー）`
   - **部品・工賃分離の原則**: 「ガラス交換」のような丸め表現を禁止。`[部品名: ガラス]` と `[技術料: ガラス交換工賃]` に行を分けて記載。
   - **全項目単価併記**: 部品代・技術料それぞれの単価を必ず明記（合計のみはNG）。
   - **B2Bリスト形式**: 管理No、貴社管理No、エンドユーザー名、時計情報を横並びにする。
   - **禁止**: ページ下部に「総合計金額」を出力しないこと。

2. **納品書（Delivery）**
   - **全項目単価併記**: 部品と技術料を分け、それぞれの金額を記載。
   - **必須項目**: `貴社管理No`、`時計Ref`、`税抜小計`、`消費税`、`送料`、`税込総合計`
   - **送料の自動化**: 送付先住所の都道府県を自動抽出し、運賃マスタ（60サイズ基本）から送料を自動入力。

3. **御請求書（Invoice）**
   - 形式: B2Bリスト形式（月次合算対応）
   - 必須項目: `請求日`、`支払期限`、`振込先口座情報`、`合計請求額`、`消費税`

4. **修理保証書（Warranty）**
   - 形式: 高級感のあるカード/ハガキサイズ（枠飾り・透かし）
   - 必須項目: `保証期間`、`シリアルNo`、`Ref/ブランド`、`保証規定`、`再来店用QRコード`

5. **QRコード**
   - 時計個体または修理IDに紐づく一意のURLを埋め込み、スキャンで即座に詳細画面へ遷移する。

### 必須アクションボタン

`見積書発行` / `納品書発行` / `請求書発行` / `保証書発行`

---

## 10. 追加機能と廃止機能の定義

### 廃止機能（V2では実装しない）

- **カンバンボード**: 一覧リスト＋詳細画面で代替するため廃止。
- **詳細操作ログ（履歴）**: 複雑化防止のため廃止。日付フィールドで進捗管理する。
- **バーコード検索・タグ即時印刷**: QRコード＋PDF印刷フローに置き換える。

### 実装機能（仕様確定）

- **部品発注管理画面（Parts Order Dashboard）**: 全修理案件から「要発注」「発注中」の部品のみを抽出して一覧表示・ステータス一括管理する専用画面。
- **詳細検索モード（Advanced Search）**: 各フィールド（顧客、ブランド、期間、ステータス、品番等）を組み合わせたAND/OR検索が可能な高度な絞り込みパネル。
- **修理事例公開（WEB Publish）**: 「WEB公開」ボタン押下時、修理情報（写真・作業内容）から個人情報を除いた「修理事例記事（ブログ形式）」を自動生成・公開する機能。マイページ機能ではない。

---

## 11. カルテ画面 UI改修仕様（2026/04/17確定）

### 基本方針
- DBは変更しない（UIのみの改修）
- 新規登録・詳細・編集を1画面に統合する
- 1変更ずつ実装すること
- 以下の完成条件チェックリストが**全て✅になるまで完成とみなさない**

---

### 現在の画面の問題点（修正必須）
- ステータスバーが存在しない
- タブ（メイン・写真・書類）が存在しない
- 書類発行ボタンの場所が不明確
- 作業明細に仕入値・上代・個数が表示されていない
- 作業明細に部品検索ボタン（🔍）がない
- AIチャット入力ボタンがない
- FMP風の左右2カラムレイアウトになっていない
- 画面密度が低くスペースが余っている

---

### 完成条件チェックリスト

#### 【A】タブ構成
- [ ] 画面上部に[メイン][写真][書類]の3タブが存在する
- [ ] 各タブをクリックすると対応するコンテンツに切り替わる
- [ ] デフォルトはメインタブが選択された状態

#### 【B】メインタブ：ヘッダー
- [ ] 管理No（例：T-006）が表示されている
- [ ] 顧客名が表示されている
- [ ] 保存ボタンが存在する

#### 【C】メインタブ：ステータスバー
- [ ] 以下のステータスが横一列に並んでいる
  　受付 → 見積中 → 部品待(未注文) → 部品待(注文済) → 作業中 → 完了 → 納品済
- [ ] 各ステータスの下に日付が表示される（時刻は不要）
- [ ] 日付部分をクリックするとカレンダーが開き日付を選択できる
- [ ] 過去日付も選択可能
- [ ] 日付が未入力のステータスは「-」と表示される
- [ ] 現在のステータスがハイライト表示される
- [ ] キャンセルは別途赤いボタンで対応
- [ ] 「履歴を見る」で変更履歴を折りたたみ展開できる

#### 【D】メインタブ：左右2カラム
- [ ] 左カラムに時計情報（ブランド・モデル・Ref・キャリバー・シリアル・付属品・外装チェック）
- [ ] 右カラムに依頼内容・内部メモ
- [ ] 左右が横並びになっている（縦積みではない）

#### 【E】メインタブ：作業明細テーブル
- [ ] 以下の列が存在する：作業カテゴリ・部品名・仕入値・上代・個数・🔍ボタン・削除ボタン
- [ ] 各行の🔍ボタンを押すと部品検索が起動する（検索機能本体は後工程でよい）
- [ ] 行を追加できる
- [ ] インライン編集ができる（各セルを直接編集）

#### 【F】メインタブ：合計欄
- [ ] 技術料合計・部品代合計・値引・消費税・税込合計が表示される
- [ ] 明細の変更に連動してリアルタイムで更新される

#### 【G】AIチャット入力
- [ ] 画面右下に[🤖 AI入力]ボタンが常時表示される
- [ ] ボタンを押すと画面下からチャット欄がスライドして出てくる
- [ ] チャット欄にはテキスト入力欄とマイクボタン（🎤）がある
- [ ] 閉じるボタンでチャット欄が閉じる
- [ ] AI連携本体は後工程でよい（UIのみ先行実装）

#### 【H】写真タブ
- [ ] 写真の一覧表示ができる
- [ ] 写真をアップロードできる

#### 【I】書類タブ
- [ ] 見積書・納品書・請求書・保証書の発行ボタンが存在する

---

### 実装順序
1. 新規・詳細・編集の3画面を1画面に統合
2. ステータスバーのコンパクト化（カレンダー選択・日付のみ）
3. タブ構成（メイン・写真・書類）
4. メインタブの左右2カラムレイアウト
5. 作業明細テーブルの列追加（仕入値・上代・個数・🔍ボタン）
6. AIチャット入力UIの組み込み
7. 書類タブの発行ボタン整備

### 各ステップの完了確認方法
各ステップ実装後に必ずPlaywright MCPでスクリーンショットを撮り、
上記チェックリストの該当項目を1つずつ目視確認すること。
「動作した」ではなく「チェックリストの条件を満たした」を完了基準とする。

---

## 12. ステータス定義（2026/04/22確定）

### 修理案件ステータス一覧

| # | ステータス | 備考 |
|---|-----------|------|
| 1 | 受付 | |
| 2 | 見積中 | |
| 3 | 承認待ち | |
| 4 | 部品待ち(未注文) | |
| 5 | 部品待ち(注文済み) | |
| 6 | 部品入荷済み | |
| 7 | 作業待ち | 部品あり・なし共通の合流地点 |
| 8 | 作業中 | |
| 9 | 作業完了 | |
| 10 | 納品済み | |
| 11 | キャンセル | |
| 12 | 保留 | 一覧デフォルト非表示 |

### ステータス遷移パターン
```
部品不要：受付→見積中→承認待ち→作業待ち→作業中→作業完了→納品済み
部品必要：受付→見積中→承認待ち→部品待ち(未注文)→部品待ち(注文済み)→部品入荷済み→作業待ち→作業中→作業完了→納品済み
```

### お客様向け表示（実装は後回し）
内部ステータスとお客様向け表示は別管理。
部品待ち系は全て「修理準備中」としてまとめて表示する。

---

## 13. 部品マスタ設計（2026/04/22確定）

### 外装部品マスタ項目
```
・ブランド（日）　修理案件から挿入
・ブランド（英）　修理案件から挿入
・モデル（日）　修理案件から挿入
・モデル（英）　修理案件から挿入
・Ref（半角、カンマ区切り）
・部品カテゴリー（選択式）
・部品名（日）（入力+ドロップダウン・DB管理）
・部品名（英）（自動補完・逆も可）
・部品Ref（半角・カンマ区切り）
・cousins番号
・画像
・サイズ（カテゴリごとテンプレあり）
・純正/FIT/合わせ
・備考1
・備考2
・価格（下代）※通貨自動変換
・掛け率（1.1〜2.0・選択式）
・価格（上代）※500円単位切り上げ自動計算
・在庫
・保管場所（自由入力　例：A-3-2）
・前回購入店（ドロップダウン・DB管理）
```

### 内装部品マスタ項目
```
・ブランド（日）　修理案件から挿入
・ブランド（英）　修理案件から挿入
・ムーブメント製造元（ブランドと異なる場合のみ・任意）
・Cal.
・ベースムーブメント製造元
・ベースムーブメントCal.
・部品カテゴリー（選択式・絞り込みあり）
　地板/受け・輪列・巻上・調速・裏まわり
・部品名（日）（入力+ドロップダウン・DB管理）
・部品名（英）（自動補完・逆も可）
・部品Ref（半角・カンマ区切り）
・cousins番号
・画像
・純正/FIT/合わせ
・備考1
・備考2
・価格（下代）※通貨自動変換
・掛け率（1.1〜2.0・選択式）
・価格（上代）※500円単位切り上げ自動計算
・在庫
・保管場所（自由入力　例：A-3-2）
・前回購入店（ドロップダウン・DB管理）
```

### 価格計算ロジック
```
通貨変換：入力値 × 為替レート → 円換算（下代）
上代計算：下代 × 掛け率 → Math.ceil(価格 / 500) * 500
例：£100 × 214.3 = ¥21,430 × 1.2 = ¥25,716 → ¥26,000
```

### 内装部品の検索ロジック
```
Cal. または ベースCal. のどちらかにヒットすれば表示
WHERE cal = '入力値' OR base_cal = '入力値'
```

### 共通部品と固有部品の管理
```
共通部品（例：ゼンマイ）→ 1レコード・Cal.とベースCal.両方を持つ
固有部品（例：切替中間車）→ 別レコード・該当Cal.のみ
```

---

## 14. 部品在庫連携ロジック（2026/04/22確定）

### 在庫数の増減タイミング
```
入荷確認時 → +1
作業待ちステータス移行時 → -1（作業完了時ではない）
```

### ステータス自動連動
```
EstimateItemに部品登録
→ orderStatus: pending
→ 案件ステータス：部品待ち(未注文)に自動変更

発注管理画面で「発注済み」ボタン押下
→ orderStatus: ordered
→ 案件ステータス：部品待ち(注文済み)に自動変更

発注管理画面で「入荷済み」ボタン押下
→ orderStatus: received
→ 部品マスタの在庫数 +1
→ 全部品がreceivedになった時のみ案件ステータス：部品入荷済みに自動変更
```

### 在庫引き当てフロー
```
部品登録時に在庫あり検知
→「在庫から引き当てますか？」確認ダイアログ表示
→ OK → received・在庫数-1・作業待ちへ
→ キャンセル → 発注フローへ
```

### 在庫不足時のフロー
```
在庫0の場合：発注フローへ
在庫はあるが案件数より少ない場合：
→ 警告ダイアログ表示
「〇〇の在庫が△個しかありません。どの案件に割り当てますか？」
・案件一覧（管理No・顧客名・時計情報・納期）
・[詳細を見る]ボタンで別タブに案件詳細を開く
・[この案件に割り当てる]ボタンで割り当て
```

---

## 15. 在庫管理仕様（2026/04/22確定）

### 棚卸
```
不定期・手動で在庫数を補正入力
差異があれば補正メモを記録
```

### 最小在庫数アラート
```
部品ごとに設定：
・アラートON/OFFスイッチ
・最小在庫数（任意の数値）
・在庫数が設定値を下回ったら警告表示
```

### 警告表示場所
```
・ダッシュボード：在庫不足リスト
・部品マスタ一覧：該当部品に警告表示
```

### 保管場所
```
自由入力（例：A-3-2）
QR/NFC：不要
```

---

## 16. 部品検索ページUI（2026/04/22確定）

### タブ構成
```
[全て] [内装] [外装]　デフォルト：全て
```

### 検索フォーム
```
【簡易検索】
キーワード（横断検索）・ブランド・モデル
部品カテゴリー・部品名・部品Ref・cousins番号
[検索]ボタン　[詳細検索 ▼]

【詳細検索（折りたたみ）】
Ref・純正/FIT/合わせ・在庫・前回購入店
保管場所・サイズ系・備考
```

### 表示切替
```
[テーブル表示 ⊞] [カード表示 ⊟]　[新規登録]
```

### テーブル表示項目
```
【内装】
ブランド | Cal. | ベースメーカー | ベースCal. | 部品名 | 上代 | 在庫 | 操作

【外装】
ブランド | モデル(英語優先・なければ日本語) | Ref | 部品名 | 部品Ref | 上代 | 在庫 | 操作
```

### カード表示
```
【内装カード】
[画像 or デフォルトアイコン]
部品名・ブランド・Cal.
ベース製造元・ベースCal.
部品Ref・cousins番号
純正/FIT/合わせ・在庫・上代
保管場所・前回購入店
[編集] [詳細]

【外装カード】
[画像 or デフォルトアイコン]
部品名・ブランド・モデル
Ref・部品Ref・cousins番号
純正/FIT/合わせ・在庫・上代
サイズ・保管場所・前回購入店
[編集] [詳細]
```

---

## 17. 部品登録・編集ページUI（2026/04/22確定）

### 基本方針
```
新規登録・編集を1画面に統合
新規登録時：[内装部品] [外装部品] を最初に選択
編集時：登録済みデータから自動判別
```

### フォームグループ構成

**内装部品**
```
【時計情報】
ブランド（日）・ブランド（英）
ムーブメント製造元（任意）
Cal.
ベースムーブメント製造元・ベースムーブメントCal.

【部品情報】
部品カテゴリー（選択式）
部品名（日）・部品名（英）（自動補完）
部品Ref・cousins番号
純正/FIT/合わせ
画像
備考1・備考2

【価格情報】
価格（下代）・通貨選択（¥/£/$/€）
掛け率（選択式1.1〜2.0）
価格（上代）自動計算・500円単位切り上げ

【在庫情報】
在庫数
最小在庫数アラート ON/OFF・設定個数
保管場所
前回購入店
```

**外装部品**
```
【時計情報】
ブランド（日）・ブランド（英）
モデル（日）・モデル（英）
Ref

【部品情報】
部品カテゴリー（選択式）
部品名（日）・部品名（英）（自動補完）
部品Ref・cousins番号
純正/FIT/合わせ
サイズ
画像
備考1・備考2

【価格情報】・【在庫情報】← 内装と同じ
```
